{
  "name": "@masters/diagnostics",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "lint": "echo diagnostics-lint-pending"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vitest": "^4.1.10"
  },
  "dependencies": {
    "@masters/domain": "workspace:*"
  },
  "exports": "./src/index.ts",
  "types": "./src/index.ts"
}
